import { LightboxView } from 'src/_examples/extra/lightbox-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: `Lightbox | Components`,
};

export default function Page() {
  return <LightboxView />;
}
