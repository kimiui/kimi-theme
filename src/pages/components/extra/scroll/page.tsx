import { ScrollbarView } from 'lib/_examples/extra/scrollbar-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: `Scrollbar | Components`,
};

export default function Page() {
  return <ScrollbarView />;
}
