import { EditorView } from 'lib/_examples/extra/editor-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Editor | Components` };

export default function Page() {
  return <EditorView />;
}
