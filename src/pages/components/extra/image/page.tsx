import { ImageView } from 'lib/_examples/extra/image-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Image | Components` };

export default function Page() {
  return <ImageView />;
}
