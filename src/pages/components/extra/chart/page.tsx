import { ChartView } from 'lib/_examples/extra/chart-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Chart | Components` };

export default function Page() {
  return <ChartView />;
}
