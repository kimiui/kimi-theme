import { AccordionView } from 'lib/_examples/mui/accordion-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Accordion | MUI` };

export default function Page() {
  return <AccordionView />;
}
