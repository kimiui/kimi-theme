import { ButtonView } from 'lib/_examples/mui/button-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Button | MUI` };

export default function Page() {
  return <ButtonView />;
}
