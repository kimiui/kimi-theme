import { AvatarView } from 'lib/_examples/mui/avatar-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Avatar | MUI` };

export default function Page() {
  return <AvatarView />;
}
