import { TreeView } from 'lib/_examples/mui/tree-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Tree view | MUI` };

export default function Page() {
  return <TreeView />;
}
