import { TextfieldView } from 'lib/_examples/mui/textfield-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Textfield | MUI` };

export default function Page() {
  return <TextfieldView />;
}
