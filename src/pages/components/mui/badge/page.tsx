import { BadgeView } from 'lib/_examples/mui/badge-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Badge | MUI` };

export default function Page() {
  return <BadgeView />;
}
