import { CheckboxView } from 'lib/_examples/mui/checkbox-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Checkbox | MUI` };

export default function Page() {
  return <CheckboxView />;
}
