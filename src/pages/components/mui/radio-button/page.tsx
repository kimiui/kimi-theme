import { RadioButtonView } from 'lib/_examples/mui/radio-button-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Radio button | MUI` };

export default function Page() {
  return <RadioButtonView />;
}
