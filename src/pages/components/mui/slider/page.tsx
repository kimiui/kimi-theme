import { SliderView } from 'lib/_examples/mui/slider-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Slider | MUI` };

export default function Page() {
  return <SliderView />;
}
