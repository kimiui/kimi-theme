import { ComponentsView } from 'lib/_examples/view';

// ----------------------------------------------------------------------

export const metadata = { title: `All components | MUI` };

export default function Page() {
  return <ComponentsView />;
}
