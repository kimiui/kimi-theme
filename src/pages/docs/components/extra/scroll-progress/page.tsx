import { ScrollProgressView } from 'kimi-theme/sections/_examples/extra/scroll-progress-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: `Scroll progress | Components`,
};

export default function Page() {
  return <ScrollProgressView />;
}
