import { MultiLanguageView } from 'src/sections/_examples/extra/multi-language-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: `Mutiple language | Components`,
};

export default function Page() {
  return <MultiLanguageView />;
}
