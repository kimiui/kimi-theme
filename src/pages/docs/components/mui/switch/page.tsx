import { SwitchView } from 'src/_examples/mui/switch-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Switch | MUI` };

export default function Page() {
  return <SwitchView />;
}
