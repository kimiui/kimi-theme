import { ListView } from 'kimi-theme/sections/_examples/mui/list-view';

// ----------------------------------------------------------------------

export const metadata = { title: `List | MUI` };

export default function Page() {
  return <ListView />;
}
