import { CheckboxView } from 'src/_examples/mui/checkbox-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Checkbox | MUI` };

export default function Page() {
  return <CheckboxView />;
}
