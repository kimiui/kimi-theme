import { DialogView } from 'kimi-theme/sections/_examples/mui/dialog-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Dialog | MUI` };

export default function Page() {
  return <DialogView />;
}
