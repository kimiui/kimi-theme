import { TimelineView } from 'src/_examples/mui/timeline-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Timeline | MUI` };

export default function Page() {
  return <TimelineView />;
}
