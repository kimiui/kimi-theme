import { PopoverView } from 'src/_examples/mui/popover-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Popover | MUI` };

export default function Page() {
  return <PopoverView />;
}
